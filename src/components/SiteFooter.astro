---
import { Image } from 'astro:assets';
import logoImg from '../assets/jake-logo.png';

const year = new Date().getFullYear();
---

<footer class="w-full max-w-[640px] mx-auto px-6 py-10 border-t border-gray-200 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
  <div class="flex items-center gap-3">
    <Image
      src={logoImg}
      alt="Jake Hartson logo."
      width={32}
      height={32}
      loading="lazy"
      class="rounded-sm"
    />
    <span class="font-sans text-sm text-gray-500">
      &copy; {year} Jake Hartson. Orange County, CA.
    </span>
  </div>
  <p class="font-sans text-sm text-gray-500">
    Not a texter?{' '}
    <a
      href="mailto:jake@jakehartson.com"
      class="underline decoration-honey underline-offset-2 hover:text-honey-dark transition-colors"
    >
      jake@jakehartson.com
    </a>
  </p>
</footer>
